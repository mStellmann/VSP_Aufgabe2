Logging einfügen
Docs schreiben

- Entwurf:
    - nodeUtil erklären
    - kein find + found -> nodeFunction and response
    - neues modulübersichtsbild
    - controller erklären -> alles mit rückgabewerten -> controller loopt
    - BestEdge, TestEdge, InBranch erklären.. Reihenfolge der Tupleelemente

- Orddict => key -> {value} ; EdgeNumber -> {NodeName,EdgeState}